<p>Адмнистраторы команды: </p>
<% if @teamadmins.empty? %>
  <li> Дополнительные администраторы не назначены</li>
<% else %>
  <% @teamadmins.each do |tmadm| %>
    <li>
      <%= Player.find(tmadm.user_id).name %>
      <a data-toggle="tooltip" data-placement="top" title="" rel="nofollow" onClick="delClick(<%= tmadm.id %>)">
        <i class="fa fa-close"></i>
      </a>
    </li>
  <% end %>
<% end %>
<br>
<p><strong><i>Добавить нового администратора?</i></strong></p>
<% if @candidates[0].nil? %>
  <li>Кандидатов нет</li>
<% else %>
  <%= render partial: "teams/new_adm", locals: {candidate: @candidates, team: @team} %>
<% end %>